# API gateway origin (browser calls this with credentials)
VITE_API_BASE_URL=http://localhost:8000

# Playwright E2E: required (no default in repo). Example: same value as SEED_SUPERADMIN_PASSWORD / Vault e2e secret.
# E2E_USER_PASSWORD=
